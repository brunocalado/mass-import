/** @type {string} The canonical module identifier — single source of truth. */
export const MODULE_ID = 'mass-import';

/** @type {number} Default elevation span assigned to each level in the scene levels importer. */
export const DEFAULT_FLOOR_HEIGHT = 10;
